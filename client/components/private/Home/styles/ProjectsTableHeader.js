import styled from 'styled-components';

const ProjectsTableHeader = styled.div`
    display: flex;
    justify-content: space-between;
    margin-bottom: 10px;
`;

const Title = styled.h2`
    font-size: 30px;
`;

const ButtonWrapper = styled.div``;

export { ProjectsTableHeader, Title, ButtonWrapper };
