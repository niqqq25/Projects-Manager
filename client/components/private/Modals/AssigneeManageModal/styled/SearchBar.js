import styled from 'styled-components';
import { searchBar } from '../../../../global/styles/searchBar';

const SearchBar = styled.input`
    ${searchBar}
    margin-bottom: 1.5rem;
`;

export { SearchBar };