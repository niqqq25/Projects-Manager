import { css } from 'styled-components';

const modalOuter = css`
    z-index: 5;
`;

const modalInner = css`
    width: 400px;
`;

const searchBar = css`
    margin-bottom: 24px;
`;

export { searchBar, modalInner, modalOuter };
