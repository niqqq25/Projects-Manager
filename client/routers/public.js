import React from 'react';
import { BrowserRouter, Switch } from 'react-router-dom';

const router = () => (
    <BrowserRouter>
        <Switch></Switch>
    </BrowserRouter>
);

export default router;
